<?php

/**
 * Implements hook_form_alter().
 */
function formui_form_alter(&$form, \Drupal\Core\Form\FormStateInterface $form_state, $form_id) {
  if (\Drupal::request()->get('debug_form_id') && \Drupal::currentUser()->hasPermission('access devel info')) {
    dpm($form_id);
  }
}
